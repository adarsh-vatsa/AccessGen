{
  "Version": "v1.0",
  "Actions": [
    {
      "Name": "InvalidAction",
      "missing_field": "This action is missing required fields",
      // Invalid JSON comment
    }
  ],
  "Resources": [
    {
      "Name": "broken_resource"
      // Missing comma and ARNFormats
  ]
  "ConditionKeys" [  // Missing colon
    {"Name": "invalid"}
  ]
}